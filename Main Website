<html>
    <title>The Secret Button</title>
    
    <center><h1>Click the button below to go to a random wikipedia page!</h1>
    
    <button onclick="randomLink();">The Secret Button</button>
    
    <h2>Click below to go to a random animal image generator</h2>
    
    <form method="get" action="http://thesecretimages.neocities.org"><button type="submit">Click Here to see a random image</button></form> 
    
    
    
    
    <h2>Send any feedback to info.thesecretbutton@gmail.com</h2>
    

    
    <script type="text/javascript">

    var randomLink = function () {

        // Array of links
        var links = [
            "en.wikipedia.org/wiki/Special:Random",
            
        ];

        // Maximum random number size
        // by counting the number of links in the array
        var max = (links.length)

        // Generate a random number
        var randomNumber = Math.floor(Math.random()*max);

        // use that random number to retrieve a link from the array
        var link = links[randomNumber];

        // change the location of the window object
        window.location = "http://" + link;
    }
   
    </script> 
      
    </center>
    
<style>
body {background-color:lightblue}
h1   {color:black}
p    {color:green}
</style>
</html>
